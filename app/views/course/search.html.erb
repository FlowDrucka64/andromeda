<h1> Course search</h1>

<%= form_tag(course_search_url, method: :get) do %>
  <%= text_field_tag(:q, params[:q]) %>
  <%= hidden_field_tag :p, 1 %>
  <div class="center"><%= submit_tag("Search") %></div>
<% end %>

<%= render "base/search", search_url:course_search_url.to_s %>
